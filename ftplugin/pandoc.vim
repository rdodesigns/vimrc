set tabstop=4    " Number of spaces that a <Tab> in the file counts for.
set shiftwidth=4 " Number of spaces to use for each step of (auto)indent

set autoindent    " New line indents to the same level as the last one.
set nosmartindent " Turn off automatically indenting more, usually wrong.
